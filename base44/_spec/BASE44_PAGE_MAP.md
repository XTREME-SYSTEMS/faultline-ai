# Base44 Page Map

## Public
`/`, `/product`, `/solutions`, `/industries`, `/how-it-works`, `/pricing`, `/resources`, `/security`, `/about`, `/contact`

## Access and payment
`/sign-in`, `/sign-up`, `/checkout`

## Customer portal
`/app`, `/app/audits`, `/app/website-intelligence`, `/app/company-discovery`, `/app/system-map`, `/app/revenue-leaks`, `/app/risk-register`, `/app/ai-readiness`, `/app/repair-plans`, `/app/business-builder`, `/app/outreach`, `/app/leads`, `/app/tasks`, `/app/projects`, `/app/reports`, `/app/monitoring`, `/app/team`, `/app/integrations`, `/app/billing`, `/app/settings`

## Internal
`/admin`
