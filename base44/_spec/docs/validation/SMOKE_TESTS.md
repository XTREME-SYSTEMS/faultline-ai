# Smoke Tests

- Load `/`, `/sign-in`, `/sign-up`, `/checkout`, `/app`, `/app/audits`, `/app/outreach`, and `/admin`.
- Confirm no missing-route page.
- Confirm hero is abstract and marketing-led.
- Confirm portal sidebar is black and workspace is white.
- Confirm checkout states that no live payment is processed.
- Confirm outreach states that approval is required.
