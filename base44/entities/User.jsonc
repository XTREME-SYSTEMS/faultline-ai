{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "organization_id": {
      "type": "string",
      "description": "The organization this user belongs to (tenant anchor for RLS)"
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}